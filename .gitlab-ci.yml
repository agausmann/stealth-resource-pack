image: "registry.gitlab.com/agausmann/stealth-resource-pack/ci"
stages:
    - build
package:
    stage: build
    script:
        - "make package"
    artifacts:
        paths:
            - "stealth.zip"
